[package]
name = "hhm-mash-web"
version = "0.1.0"
edition = "2021"
rust-version = "1.82"
license = "MIT"
description = "Maud + Axum + SeaORM + Supabase/PostgreSQL + HTMX + WebSocket web server for Hacker House Medellín."
repository = "https://github.com/hacker-house-medellin/hhm-mash-web"
publish = false

[dependencies]
axum = { version = "0.8", features = ["form", "ws"] }
futures-util = "0.3"
maud = { version = "0.27", features = ["axum"] }
sea-orm = { version = "2.0", features = ["runtime-tokio-rustls", "sqlx-postgres"] }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
tokio = { version = "1", features = ["macros", "rt-multi-thread", "net", "sync"] }
tower-http = { version = "0.6", features = ["trace"] }
tracing = "0.1"
tracing-subscriber = { version = "0.3", features = ["env-filter"] }
uuid = { version = "1", features = ["v4"] }

[profile.release]
lto = "thin"
codegen-units = 1
strip = "symbols"
